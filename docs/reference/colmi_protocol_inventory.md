# Colmi / QRing protocol inventory

This is the ledger for Colmi / QRing protocol work. Fill it before changing
current HR, SpO2, or other biometric-adjacent behavior.

The dangerous failure mode is not "we failed to read a value." The dangerous
failure mode is a bridge that sounds more certain than it is.

## Rules for inventory work

- Do not propose implementation changes while filling the ledger.
- Do not collapse live/manual/history/cached reads into one bucket.
- Quote or cite the source artifact for every claim.
- Mark unknowns as `unknown`; do not infer silently.
- Track freshness implications separately from parser mechanics.
- Treat vendor UI behavior as evidence, not truth. The vendor app may also be
  presenting cached values as current.
- Prefer one boring row per artifact over a clever summary.

## Source artifact ledger

| Artifact / class / file | Source kind | Chinese / vendor terms | Related request id(s) | Payload shape | Response / parser fields | Lifecycle / timing / retry behavior | Live, manual, history, or cache? | Freshness implication | SymbioSync parity | Confidence | Source quote / notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `symbiosync/devices/colmi.py` `request_realtime_hr()` | current implementation | n/a | `0x69` | `[0x69, 0x01, 0x01, ...]` | notify handler: `data[1]==0x01`, `data[2]==0` (error_code), `data[3]` as BPM | Starts stream on connect; keepalive re-requests every 45s if no recent response; snapshot starts stream and waits for first valid response (timeout 45s) | presumed live stream | current only if a notification timestamp is inside the freshness window (5s default) | implemented, but repeated identical values need protocol review | medium | Existing behavior, not independent protocol proof. |
| `symbiosync/devices/colmi.py` `request_stop_hr()` | current implementation | n/a | `0x6A` | `[0x6A, 0x01, 0x00, 0x00, ...]` | no response expected | Sent on disconnect and before SpO2 start | n/a (stop command) | n/a | implemented | medium | Existing behavior. |
| `symbiosync/devices/colmi.py` `request_spo2_start()` / `request_spo2_stop()` | current implementation | n/a | `0x69` / `0x6A` | `[0x69, 0x03, 0x01, ...]` / `[0x6A, 0x03, 0x00, 0x00, ...]` | notify handler: `data[1]==0x03`, `data[3]` as SpO2% | SpO2 pauses HR, measures, stops, resumes HR; frequent cycling has correlated with flaky BLE; background cycle: 3s measurement window | presumed manual/live measurement | current only if notification timestamp is inside SpO2 window (300s default) | implemented with caution; protocol still needs external confirmation | medium-low | Existing behavior and field observation. |
| `symbiosync/devices/colmi.py` `_notification_handler()` | current implementation | n/a | `0x69` (start realtime), `0x48` (today sports), `0x03` (battery), `0x01` (set time) | UART TX notify | `request_id = data[0] & 0x7F`; for `0x69`: `reading_type=data[1]`, `error_code=data[2]`, `value=data[3]` | Continuous notification handler; processes each packet as it arrives | depends on reading_type | depends on reading_type | implemented | medium | Parser byte mapping is from current code, not independently verified against vendor response spec. |
| `symbiosync/devices/colmi.py` `_metric_snapshot()` | current implementation | n/a | n/a | n/a | Returns `ok`, `value`, `captured_at`, `age_seconds`, `freshness`, `source` | Freshness window: HR 5s, SpO2 300s, steps 120s | n/a (freshness function) | Defines current/stale/unavailable boundaries | implemented; HR 5s window may be too tight for ring notification cadence | medium | The freshness windows are SymbioSync design choices, not vendor specs. |
| `symbiosync/devices/colmi.py` `_snapshot_hr()` | current implementation | n/a | `0x69` | Writes `request_realtime_hr()`, polls for new `_last_hr_response` | Waits for `_heart_rate > 0` and `_last_hr_response > prev_response` | Timeout 45s; polls every 0.5s | manual/live (on-demand) | current if response arrives within timeout | implemented | medium | Does not stop HR stream after reading; stream continues. |
| `symbiosync/devices/colmi.py` `_snapshot_spo2()` | current implementation | n/a | `0x6A` / `0x69` | Writes `request_stop_hr()`, then `request_spo2_start()` | Waits for `_last_spo2 > prev_spo2` | Timeout 10s; polls every 0.5s; then sends `request_spo2_stop()` and resumes HR | manual/live (on-demand) | current if response arrives within timeout | implemented | medium-low | HR pause/resume is SymbioSync's addition, not confirmed from vendor app. |
| `symbiosync/devices/colmi.py` `_run_spo2_cycle()` | current implementation | n/a | `0x6A` / `0x69` | Writes `request_stop_hr()`, `request_spo2_start()`, sleeps 3s, `request_spo2_stop()`, then `_resume_hr_with_retry()` | Relies on notification handler to update `_spo2` | Runs every 3600s; detached background task; 3s measurement window | manual/live (periodic) | current if notification arrives during 3s window | implemented; 3s window may be too short for some rings | medium-low | "Wrapping SpO2 with HR_STOP/HR_START is critical: the ring cannot stream HR and measure SpO2 simultaneously" (code comment). |
| `symbiosync/devices/colmi.py` `_resume_hr_with_retry()` | current implementation | n/a | `0x69` | Writes `request_realtime_hr()`, waits 5s, checks `_last_hr_response` | Up to 3 attempts | After SpO2_STOP the ring can go silent; retry prevents BLE supervision timeout | n/a (recovery) | n/a | implemented; 5s verify_wait, 3 attempts | medium | Field-tested behavior, not vendor spec. |
| `reference/colmi_inferred_operations/BIOMETRIC_PROTOCOL.md` | curated APK static-analysis notes | `StartHeartRateReq.java`, `StopHeartRateReq.java`, `StartHeartRateRsp.java` | `0x69`, `0x6A` | `0x69` starts realtime; `0x6A` stops; `getSimpleReq(byte b)` builds `(type=b, action=0)` for types <3; `getRealtimeHeartRate(byte b)` builds `(type=6, action=b)` | `StartHeartRateRsp`: `type`, `errCode`, `value`, optional `sbp/dbp` | unknown (APK static analysis does not reveal runtime timing) | APK supports same current-measurement model SymbioSync uses | unknown (APK may present cached values in UI) | HR start/stop/SpO2 start/stop payloads match SymbioSync | high | "The remaining reliability question is lifecycle/timing/retry behavior, not a completely different obvious one-shot command found so far." |
| `reference/colmi_inferred_operations/BIOMETRIC_PROTOCOL.md` (StopHeartRateReq) | curated APK static-analysis notes | `StopHeartRateReq.java` | `0x6A` | stop types: 1=HR, 2=blood pressure, 3=SpO2, 4=fatigue, 5=health check, 7=ECG, 8=pressure, 9=blood sugar, 10=HRV, 11=temperature | n/a | unknown | n/a | n/a | SymbioSync uses stop types 1 (HR) and 3 (SpO2); other types not implemented | high | Vendor stop types confirmed. |
| `reference/colmi_inferred_operations/BIOMETRIC_PROTOCOL.md` (BigData biometric ids) | curated APK static-analysis notes | `LargeDataHandler.java` | BigData: `0x27`, `0x28`, `0x49`, `0x5F`, `0x75`, `0x77` | BigData request shapes: sleep `addHeader(39, [day_selector, 1])`; manual HR `addHeader(40, [0 or 0xff])`; interval SpO2 `addHeader(95, [day_index, packet_index])`; interval HR `addHeader(117, [day_index, packet_index])` | Manual HR/SpO2: offset 6=index, offset 7+ = triplets (2 bytes minute/index + 1 byte value). Interval HR/SpO2: offset 6=day index, 7=interval min, 8=packet count, 9=packet index, 10+=values as u8 | Interval data: recursive request if packet count indicates more packets | history (manual and interval are stored device data, not live) | Cannot be treated as current body-state; timestamps/index semantics unvalidated | Not implemented in SymbioSync | medium | "BigData manual/interval HR/SpO2 are historical or stored-series reads, not proven current reads." |
| `reference/colmi_inferred_operations/BIOMETRIC_PROTOCOL.md` (static-analysis oddity) | curated APK static-analysis notes | `syncIntervalHeartRate` | BigData: `0x75` | n/a | n/a | `syncIntervalHeartRate(int i, ILargeDataResponse cb)` calls `syncIntervalBloodOxygenReal(i, 0, cb)` in APK-derived reference; but `syncIntervalHeartRateReal(i, packet, cb)` exists and correctly sends id `117` | n/a | n/a | n/a | low | "Treat this as static-analysis/app weirdness until validated on hardware." |
| `reference/colmi_inferred_operations/prior_findings/Obsidian_FINDINGS.md` | prior sleep scoring analysis | `AlSleepUtil.calcSleepScore` | n/a | n/a | Sleep score: 70% weight total duration (target 500 min), 15% free awake-bonus (always called with 0 wakeups), 15% composition | n/a | n/a | n/a | SymbioSync `calc_sleep_score()` matches APK formula | high | "The awake penalty is vestigial. Always called with 0, so everyone gets +15 free points." |
| `reference/colmi_inferred_operations/prior_findings/Obsidian_FINDINGS.md` (sleep type codes) | prior sleep analysis | `SleepNewProtoResp.java` | BigData: `0x27` | n/a | New-protocol sleep types: 0=NODATA, 1=ERROR, 2=light, 3=deep, 4=REM, 5=awake | n/a | n/a | n/a | SymbioSync `SleepType` enum matches new-protocol numbering | high | "Old protocol has 1=deep, 2=light. New protocol has 2=light, 3=deep. Our colmi.py SleepType enum uses new-protocol numbering (correct for R02)." |
| `reference/colmi_inferred_operations/prior_findings/Obsidian_FINDINGS.md` (ring firmware classification) | prior field observation | n/a | n/a | n/a | n/a | Ring firmware is sole sleep classifier; no config commands tune sensitivity; sleep schedule not sent to ring | n/a | n/a | n/a | high | "The ring firmware has no knowledge of when you 'should' be sleeping." |
| `reference/colmi_inferred_operations/prior_findings/Obsidian_FINDINGS.md` (SpO2 immediate trigger) | prior field observation | n/a | n/a | n/a | n/a | `_last_spo2_cycle` was changed from `time.time()` to `0.0` on connect so first SpO2 fires on first keepalive (~12s) instead of waiting 60s | n/a | n/a | Implemented in SymbioSync | medium | Code change applied; ring often disconnected before 60s timer fired. |
| `reference/colmi_inferred_operations/MANIFEST.md` | curated reference manifest | n/a | n/a | n/a | n/a | n/a | n/a | n/a | Confirms `colmi.py` aligned with APK on core UUIDs, 16-byte command checksum, set time, battery, today sports, HR start/stop, SpO2 start/stop, sleep score | high | Lists additional BigData surfaces not yet implemented. |
| `plugins/colmi/README.md` | plugin documentation | n/a | n/a | n/a | n/a | n/a | n/a | n/a | Documents current-read semantics, BLE reliability notes, SpO2 opt-in rationale | high | "SpO2 is opt-in because it can interrupt HR streaming and has correlated with BLE flakiness." |
| `docs/TRUTHFULNESS_AUDIT.md` | truthfulness audit | n/a | n/a | n/a | n/a | n/a | n/a | n/a | Documents truth distinctions: current vs stale, missing vs zero, connected vs remembered, accepted vs observed | high | "The dangerous failure mode is a bridge that sounds more certain than it is." |
| `scripts/quarantine_colmi_hr_readings.py` | local repair helper | n/a | n/a | n/a | n/a | Copies suspect HR rows to `suspect_realtime_heart_rate` table, removes from `realtime_heart_rate` | n/a | n/a | n/a | medium | Used to quarantine repeated HR `97` rows. Does not destroy data. |
| `com.oudmon.ble.base.communication.rsp.RealTimeHeartRateRsp` (decompiled4) | APK response class | `RealTimeHeartRateRsp.java` | `0x1E` (30) | n/a (device-initiated notification) | `bArr[0]` = HR value (maps to `data[1]` in raw packet) | Single-packet; `acceptData` returns `false` immediately | **device-initiated live notification** | **value at data[1], NOT data[3]** — different payload layout than StartHeartRateRsp | Not implemented separately; SymbioSync reads data[3] for all notifications | **medium-high** | ⚠️ CRITICAL: If ring sends realtime HR via cmd 0x1E, SymbioSync reads wrong byte. `data[1]` is the HR value for this notification type, not `data[3]`. This could explain repeated-97 anomaly. |
| `com.oudmon.ble.base.bluetooth.BeanFactory` (decompiled4) | APK command dispatch factory | `BeanFactory.java` | all small-data IDs | n/a | Maps command IDs to `BaseRspCmd` subclasses; full map: 1→SetTime, 3→Battery, 7→TotalSport, 12→BPSetting, 13→BPData, 20→ReadBlePressure, 21→ReadHeartRate, 22→HeartRateSetting, 30→RealTimeHR, 44→BloodOxygenSetting, 54→PressureSetting, 55→Pressure, 56→HRVSetting, 57→HRV, 60→DeviceSupport, 67→ReadDetailSport, 68→ReadSleepDetails, 105→StartHeartRate, 115→DeviceNotify | n/a | n/a | n/a | Confirms command ID mapping; RealTimeHeartRateRsp at 30 is a distinct notification type | high | Full BeanFactory map now documented in decompiled_walkthrough_2026-05-18.md |
| `com.oudmon.ble.base.communication.Constants` (decompiled4) | APK protocol constants | `Constants.java` | n/a | n/a | UART Service: `6e40fff0-b5a3-f393-e0a9-e50e24dcca9e`; Notify: `6e400003-...`; Write: `6e400002-...`; LargeData Service: `de5bf728-...`; LargeData Notify: `de5bf729-...`; LargeData Write: `de5bf72a-...`; Packet length: 16; High-bit mask: 128 (0x80) | n/a | n/a | n/a | Confirms SymbioSync UUID alignment; confirms 0x80 high-bit masking | high | |
| `com.oudmon.ble.base.communication.LargeDataHandler` (decompiled4) | APK BigData command builder & parser | `LargeDataHandler.java` | BigData: 32,39,40,42,44,45,46,47,58,71,72,73,74,95,117,118,119 | `addHeader(cmdId, payload)`: `[0xBC, cmdId, len(u16le), crc16(u16le), ...payload...]` | Full BigData command map documented; manual HR=40, manual SpO2=73, interval SpO2=95, interval HR=117, interval temp=119, sleep night=39, sleep lunch=62 | Recursive multi-packet for interval data; sleep has night+nap channels | history (all BigData surfaces are stored device data) | Cannot be current body-state | Not implemented in SymbioSync | high | Complete LargeDataHandler map now in decompiled_walkthrough_2026-05-18.md |
| `com.oudmon.ble.base.communication.req.StopHeartRateReq` (decompiled4) | APK stop command | `StopHeartRateReq.java` | `0x6A` | `[0x6A, type, param, 0x00, ...CRC]` | Stop types: 1=HR, 2=BP, 3=SpO2, 4=fatigue, 5=health check, 7=ECG, 8=pressure, 9=blood sugar, 10=HRV, 11=temperature | n/a | n/a | n/a | SymbioSync uses types 1 and 3; other types not implemented | high | Confirms and extends prior findings with full type list |
| `com.oudmon.ble.base.communication.req.StartHeartRateReq` (decompiled4) | APK start command | `StartHeartRateReq.java` | `0x69` | `[0x69, type, param, ...CRC]` | `getSimpleReq(b)`: type=b, param=0 for b<3, param=BCD(25) for b>=3; `getRealtimeHeartRate(b)`: type=6, param=b; `getEcgReqStartAndStop(z)`: type=16 | n/a | n/a | n/a | SymbioSync sends `[0x69, 0x01, 0x01]` which matches `getSimpleReq(1)` but with param=1 not 0 | medium-high | SymbioSync's second byte is 0x01 not 0x00; difference may be intentional (start vs query) or a minor discrepancy |
| `com.oudmon.ble.base.communication.LargeDataHandler` — manual SpO2 (decompiled4) | APK BigData manual SpO2 | `LargeDataHandler.java` | BigData: `0x49` (73) | Request: `syncManualBloodOxygenList(dayIndex)` — no explicit outbound addHeader shown; response registered at cmdId 73 | offset 6=index, offset 7+=triplets (2 bytes minute + 1 byte value) — same layout as manual HR | n/a | history | n/a | Not implemented | medium | Prior finding said APK reference may be incomplete; now confirmed: `syncManualBloodOxygenList` registers response at cmdId 73 but outbound request shape still unclear from decompiled code |
| `com.oudmon.ble.base.communication.LargeDataHandler` — interval HR bug (decompiled4) | APK bug confirmation | `LargeDataHandler.java` | BigData: `0x5F` (95) vs `0x75` (117) | `syncIntervalHeartRate(i, cb)` calls `syncIntervalBloodOxygenReal(i, 0, cb)` which sends cmdId **95** (SpO2) instead of `syncIntervalHeartRateReal` which sends cmdId **117** (HR) | n/a | n/a | n/a | **Confirmed bug**: interval HR request would get SpO2 data back; must use cmdId 117 | high | |
| `com.oudmon.ble.base.communication.LargeDataHandler` — sleep lunch nap (decompiled4) | APK BigData sleep lunch | `LargeDataHandler.java` | BigData: `0x3E` (62) | `syncSleepList` registers two callbacks: cmdId 39 (night sleep) + cmdId 62 (lunch nap) | `parseDaySleepLunch`: same structure as night sleep but with `LunchSleepBean` entries (startTime, endTime) | n/a | history | n/a | Not implemented | medium | Second sleep channel for daytime naps |
| `com.oudmon.ble.base.communication.rsp.DeviceSupportFunctionRsp` (decompiled4) | APK capability flags | `DeviceSupportFunctionRsp.java` | `0x3C` (60) | 9-byte bitmask | ~30 boolean capability flags covering HR, SpO2, BP, HRV, pressure, temperature, etc. | n/a | n/a | n/a | Not implemented; should query on connect to determine ring capabilities | medium | Flag positions are obfuscated; exact mapping requires runtime testing |
| `com.oudmon.ble.base.communication.JPackageManager` (decompiled4) | APK packet length manager | `JPackageManager.java` | n/a | n/a | Default 20 bytes; updated by `PackageLengthRsp` (cmd 47) | n/a | n/a | n/a | Not implemented; SymbioSync uses fixed 20-byte MTU | low | Could improve throughput if ring supports larger packets |

## Request / response matrix

| Request name | Request id | Start / stop / read | Payload bytes | Notify characteristic | Expected response bytes | Parser | Success condition | Failure / timeout behavior | Freshness boundary | Open questions |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Realtime HR start | `0x69` | start stream | `[0x69, 0x01, 0x01, ...pad..., CRC]` | UART TX (`6E400003-...`) | `data[0]&0x7F == 0x69`, `data[1]==0x01`, `data[2]==0x00`, `data[3]==BPM` | `reading_type=data[1]`, `error_code=data[2]`, `value=data[3]`; range check: `30 < value < 220` | new response timestamp and plausible BPM | timeout if no newer response (45s for snapshot, 30s active-wait for keepalive) | default current window: 5s | Does repeated same BPM represent new measurement, cached stream value, or parser error? Is `data[3]` always the BPM, or can it be a status/type byte in some notification variants? |
| Realtime HR stop | `0x6A` | stop stream | `[0x6A, 0x01, 0x00, 0x00, ...pad..., CRC]` | n/a (no response expected) | n/a | n/a | n/a | n/a | n/a | Does the ring send any acknowledgment of HR stop? SymbioSync assumes none. |
| Realtime SpO2 start | `0x69` | start measurement | `[0x69, 0x03, 0x01, ...pad..., CRC]` | UART TX (`6E400003-...`) | `data[0]&0x7F == 0x69`, `data[1]==0x03`, `data[2]==0x00`, `data[3]==SpO2%` | `reading_type=data[1]`, `error_code=data[2]`, `value=data[3]`; range check: `50 < value <= 100` | new response timestamp and plausible SpO2 | timeout if no newer response (10s for snapshot, 3s for background cycle) | default current window: 300s when requested | Does SpO2 require HR stop? What exact lifecycle does vendor app use? How long should the measurement window be? |
| Realtime SpO2 stop | `0x6A` | stop measurement | `[0x6A, 0x03, 0x00, 0x00, ...pad..., CRC]` | n/a (no response expected) | n/a | n/a | n/a | n/a | n/a | Does the ring send any acknowledgment of SpO2 stop? |
| Battery | `0x03` | read | `[0x03, ...pad..., CRC]` | UART TX | `data[0]&0x7F == 0x03`, `data[1]==battery%`, `data[2]==charging` | `battery=data[1]`, `charging=bool(data[2])` | valid battery percentage | n/a | slow-moving; polled every 300s | n/a |
| Today sports | `0x48` | read | `[0x48, ...pad..., CRC]` | UART TX | `data[0]&0x7F == 0x48`, `data[1..3]=steps`, `data[7..9]=calories`, `data[10..12]=distance` | 3-byte big-endian fields for steps, calories, distance | valid step count | n/a | polled every 60s | n/a |
| Set time | `0x01` | write | `[0x01, BCD_year, BCD_month, BCD_day, BCD_hour, BCD_min, BCD_sec, 0x01, ...CRC]` | UART TX | `data[0]&0x7F == 0x01` | emits RING_TIME_SET event | n/a | n/a | n/a | n/a |
| BigData sleep | `0xBC 0x27` | read | `[0xBC, 0x27, 0x00, 0x00, 0xFF, 0xFF]` (SymbioSync) or `addHeader(39, [day_selector, 1])` (APK) | BigData notify (`DE5BF729-...`) | BigData header: `0xBC, 0x27, len(u16le), crc16(u16le)`, then payload with sleep days and segments | `parse_sleep_packets()` in colmi.py | valid parsed sleep data with segments | timeout 10s for first packet, 2s between packets | n/a (historical) | SymbioSync uses empty request; APK uses `day_selector` argument. Does the empty request return all available days? |
| BigData manual HR | `0xBC 0x28` (40) | read | `addHeader(40, [0 or 0xff])` | BigData notify | offset 6=index, offset 7+=triplets (2 bytes minute/index + 1 byte value) | not implemented in SymbioSync | n/a | n/a | n/a (historical) | Historical data; not current body-state. Should be harvested as background, not exposed as current. |
| BigData manual SpO2 | `0xBC 0x49` (73) | read | unknown / may be incomplete in APK | BigData notify | same triplet layout as manual HR | not implemented in SymbioSync | n/a | n/a | n/a (historical) | "APK-derived method reference does not show an outbound addHeader(73, ...); may be incomplete/static-analysis artifact or called differently." |
| BigData interval HR | `0xBC 0x75` (117) | read | `addHeader(117, [day_index, packet_index])` | BigData notify | offset 6=day index, 7=interval min, 8=packet count, 9=packet index, 10+=values as u8; recursive if more packets | not implemented in SymbioSync | n/a | n/a | n/a (historical) | "syncIntervalHeartRate() calls syncIntervalBloodOxygenReal() in APK; but syncIntervalHeartRateReal() exists and uses id 117. Treat as static-analysis weirdness." |
| BigData interval SpO2 | `0xBC 0x5F` (95) | read | `addHeader(95, [day_index, packet_index])` | BigData notify | same layout as interval HR | not implemented in SymbioSync | n/a | n/a | n/a (historical) | Should be implemented only after HR stability is understood. |
| Realtime HR notification (device-initiated) | `0x1E` (30) | notify | n/a (device pushes) | UART TX (`6E400003-...`) | `data[0]&0x7F == 0x1E`, `data[1]` = HR value | `value = data[1]` (NOT data[3]) | valid HR value at data[1] | n/a | n/a | ⚠️ SymbioSync reads data[3] for all notifications; if ring uses cmd 0x1E for periodic HR, value is at wrong offset. This could explain repeated-97 anomaly. |
| HR setting read/write | `0x16` (22) | read/write | `[0x16, 1, ...CRC]` (read) or `[0x16, 2, enable, interval, ...CRC]` (write) | UART TX | `data[1]=enable`, `data[2]=interval`, `data[3]=startInterval`, `data[4]=tooLow`, `data[5]=tooHigh` | MixtureRsp pattern | n/a | n/a | Not implemented; could configure ring HR interval |
| HRV data | `0x39` (57) | read | `[0x39, action, ...CRC]` | UART TX | Multi-packet: init(count, range), first(dayOffset+values), continuation(13 bytes each) | `hrvArray`, `offset`, `range` | n/a | n/a | Not implemented |
| HRV setting | `0x38` (56) | read/write | `[0x38, 1, ...CRC]` (read) or `[0x38, 2, enable, ...CRC]` (write) | UART TX | `data[1]=enable` | MixtureRsp | n/a | n/a | Not implemented |
| SpO2 setting | `0x2C` (44) | read/write | `[0x2C, 1, ...CRC]` (read) or `[0x2C, 2, enable, interval, ...CRC]` (write) | UART TX | `data[1]=enable`, `data[2]=interval` | MixtureRsp | n/a | n/a | Not implemented; could configure ring SpO2 interval |
| Pressure data | `0x37` (55) | read | `[0x37, action, ...CRC]` | UART TX | Multi-packet: same pattern as HRV | `pressureArray`, `offset`, `range` | n/a | n/a | Not implemented |
| BP history | `0x14` (20) | read | `[0x14, timestamp(4 bytes), 0x00, 0x32, ...CRC]` | UART TX | Per-record: 4-byte timestamp + sbp + dbp; up to 50 records; 0xFFFFFFFF = no data | `ReadBlePressureRsp.valueList` | n/a | n/a | Not implemented |
| Sleep detail (old protocol) | `0x44` (68) | read | `[0x44, dayOffset, 15, startIndex, endIndex, ...CRC]` | UART TX | Multi-packet: 0xF0 init, then BCD date + timeIndex + 8 sleep quality bytes | `ReadSleepDetailsRsp.bleSleepDetailses` | n/a | n/a | Not implemented; new protocol (BigData cmdId 39) preferred |
| BigData sleep lunch nap | `0xBC 0x3E` (62) | read | Sent alongside cmdId 39 sleep request | BigData notify | Same as night sleep but with `LunchSleepBean` entries | `parseDaySleepLunch` | n/a | n/a | Not implemented; second sleep channel for daytime naps |
| BigData interval temperature | `0xBC 0x77` (119) | read | `addHeader(119, [day_index, packet_index])` | BigData notify | offset 6=dayIndex, 7=interval, 8=packet count, 9=packet index, 10+=2-byte LE values (÷100 = °C) | `IntervalTemperatureEntity` with `List<Float>` | n/a | n/a | Not implemented |
| Device support function | `0x3C` (60) | read | `[0x3C, ...CRC]` | UART TX | 9-byte bitmask with ~30 capability flags | `DeviceSupportFunctionRsp` | n/a | n/a | Not implemented; should query on connect |
| Packet length negotiation | `0x2F` (47) | read | n/a | UART TX | Updates `JPackageManager.setLength()` | `PackageLengthRsp` | n/a | n/a | Not implemented; SymbioSync uses fixed 20-byte MTU |

## Field anomalies to preserve

| Date / context | Observation | Why it matters | Current handling | Follow-up |
| --- | --- | --- | --- | --- |
| 2026-05-16/17 local testing | Thousands of repeated HR `97` rows accumulated in `data/ring_data.sqlite`. | Repeated identical biometric-adjacent values may be cached, mis-parsed, or stale. They must not be treated as reliable body-state history without protocol proof. | Rows with reading `97` were quarantined into `suspect_realtime_heart_rate` by `scripts/quarantine_colmi_hr_readings.py`, preserving a DB backup. | Identify whether `97` can be a legitimate repeated live value or indicates protocol/parser/lifecycle error. Specifically: can `data[3]==0x61` (97 decimal) be a payload type/status byte rather than a body-state value? |
| 2026-04-03 field observation | Ring often disconnected before 60s SpO2 cycle timer fired; SpO2 readings rarely captured. | SpO2 measurement window and timing affects whether current SpO2 is ever available. | `_last_spo2_cycle` initialization changed from `time.time()` to `0.0` to trigger on first keepalive (~12s). | Validate SpO2 measurement window duration. Is 3s enough? Is 10s snapshot timeout enough? |
| 2026-04-03 field observation | After SPO2_STOP, ring goes silent (no HR traffic), triggering BLE supervision timeout. | HR resume after SpO2 is critical for connection stability. | `_resume_hr_with_retry()` added: up to 3 attempts, 5s verify wait each. | Is this the same behavior the vendor app uses? Unknown. |
| APK static analysis | `syncIntervalHeartRate()` calls `syncIntervalBloodOxygenReal()` instead of `syncIntervalHeartRateReal()`. | Could indicate a bug in the vendor app, or that the static analysis is misleading. | Not implemented in SymbioSync. | Validate on hardware before implementing interval HR. |

## Implementation parity checklist

- [x] Inventory vendor/decompiled HR request classes.
- [x] Inventory vendor/decompiled SpO2 request classes.
- [x] Inventory notification parser paths for HR and SpO2.
- [ ] Inventory app lifecycle around manual/on-demand measurement.
- [ ] Compare vendor retry/timeout behavior to SymbioSync keepalive and snapshot behavior.
- [ ] Decide whether repeated identical values require explicit suspicion flags.
- [ ] Update UI/API copy so status never presents cached/stale body-state as current.
- [ ] Add tests for freshness semantics before public claims about live biometric state.
- [ ] Implement/test BigData interval HR (`0x75`) as background historical harvest.
- [ ] Implement/test BigData interval SpO2 (`0x5F`) after HR stability is understood.
- [ ] Implement/test BigData manual HR (`0x28`) as background historical harvest.
- [ ] Investigate BigData manual SpO2 (`0x49`) -- APK reference may be incomplete.
- [ ] Investigate RealTimeHeartRateRsp (cmd 0x1E) vs StartHeartRateRsp (cmd 0x69) — does the ring use 0x1E for periodic HR notifications? If so, value is at data[1] not data[3].
- [ ] Query DeviceSupportFunctionRsp (cmd 0x3C) on connect to determine ring capabilities.
- [ ] Implement BigData interval temperature (`0x77`) as background historical harvest.
- [ ] Implement BigData sleep lunch nap (`0x3E` / 62) alongside night sleep (`0x27` / 39).
- [ ] Negotiate packet length via PackageLengthRsp (cmd 47) for improved throughput.

## Terms

- **Current**: received inside the endpoint's stated freshness window.
- **Stale**: value exists but is older than the freshness window or lacks capture-time proof.
- **Unavailable**: no valid value is known.
- **Live stream**: notification path that appears to push new measurements over time.
- **Manual measurement**: explicit start/read operation intended to produce a fresh value.
- **History read**: stored device history; useful, but not current body state.
- **Cached value**: previously observed value retained by app, device, or bridge.
- **Accepted**: request was sent/transport-acknowledged, but not necessarily observed.
- **Observed**: a new value was received with a fresh timestamp, within the freshness window.
